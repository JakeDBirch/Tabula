node_modules/
.DS_Store
*.swp
*.swo
*~

# Editor
.vscode/
.idea/

# Claude Code — commit launch.json + settings.json (shared), ignore local overrides
.claude/settings.local.json

# Build scratch
.build/
*.tmp

# Stray nested clone (do not track as a submodule)
/Tabula/

# Local mobile test harness (never commit)
test-landscape.html
test-mobile.html
_testserver.js

# iOS — both generated. ios/www is re-emitted by `npm run build:ios` (it is a
# ~9MB copy of samples/, so tracking it would double the repo for nothing), and
# the Xcode project is generated from ios/project.yml by XcodeGen.
ios/www/
ios/*.xcodeproj
ios/*.xcworkspace
ios/build/
ios/DerivedData/
